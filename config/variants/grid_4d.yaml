# Variant: grid tag for the grid-native SSMs (4d) -- s4nd, mamba_nd.
# These models contain NO attention: S4ND mixes with separable DPLR kernels and
# Mamba-ND with an ordered scan. The tag only selects how many lattice axes are
# promoted: State+Commodity+Flow. Formerly (mis)named cross_attention_4d.

model:
  variant: grid_4d

# Do not add a 'data:' block here (an empty one parses as None and wipes base).
