# Variant: LOCAL reimplementation of CaFA's factorized attention operator (2d).
# Kronecker-factorized per-axis SELF-attention (LocalFactorizedAttention),
# drop-in for AxialComboSA. Axes: State.
# Softmax kernels, locally written projections -- see src/models/fa_local.py.
# The authors' own components live in the fa_2d variant instead.

model:
  variant: fa_local_2d

  attention:
    dropout: 0.0

# Do not add a 'data:' block here (an empty one parses as None and wipes base).
